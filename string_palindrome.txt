# find  palindrome numbers
def  palindrome(s):
    start = 0
    end = len(s) - 1
    
    while start < end:
        if s[start] != s[end]:
            return False
        end -= 1
        start +=1
            
    return True
    
        
print(palindrome("abbaa"))

#time complexity O(n)
        