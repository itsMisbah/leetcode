def find_duplicate(arr):
    seen = {}        # dictionary!
    duplicate = []
    
    for i in arr:
        if i in seen:
            duplicate.append(i)
        else:
            seen[i] = 1
        
    return duplicate

print(find_duplicate([1,2,3,2,4,3,5]))
# time complexity O(n) 